import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    text: {
      primary: '#fff',
    },
    background: {
      default: '#03000f',
      paper: '#0a0329',
    },
  },
  typography: {
    fontFamily: 'Inter',
  },
})
